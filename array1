// C function to find maximum in arr[] of size n
    int largest(int arr[], int n) {
        int i;
        int max = arr[0];
        for(i=0;i<n;i++)
        if (arr[i]>max)
        max= arr[i];
        return max;
}
